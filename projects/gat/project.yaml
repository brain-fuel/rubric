# Bricks this project includes. Keep this in sync with the imports in main.go;
# `goforge check` verifies that declared bricks match the ones actually imported.
bricks:
  - cli
  - assets
  - config
  - controller
  - decorations
  - gitdiff
  - inputsrc
  - linerange
  - pager
  - printer
  - style
  - termdetect
